# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build tiktok-content-assistant`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t tiktok-content-assistant`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system tiktok-content-assistant && \
          adduser --system -G tiktok-content-assistant tiktok-content-assistant

COPY dist/apps/tiktok-content-assistant tiktok-content-assistant/
RUN chown -R tiktok-content-assistant:tiktok-content-assistant .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix tiktok-content-assistant --omit=dev -f install

CMD [ "node", "tiktok-content-assistant" ]
